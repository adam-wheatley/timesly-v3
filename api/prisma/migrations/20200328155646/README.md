# Migration `20200328155646`

This migration has been generated by adam-wheatley at 3/28/2020, 3:56:46 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;

CREATE TABLE "quaint"."new_User" (
    "company" TEXT NOT NULL  ,
    "dateOfBirth" DATE   ,
    "email" TEXT NOT NULL DEFAULT '' ,
    "firstName" TEXT   ,
    "id" TEXT NOT NULL  ,
    "isActive" BOOLEAN NOT NULL DEFAULT true ,
    "isAdmin" BOOLEAN NOT NULL DEFAULT false ,
    "lastName" TEXT   ,
    "password" TEXT NOT NULL DEFAULT '' ,
    "tokenVersion" INTEGER NOT NULL DEFAULT 0 ,
    PRIMARY KEY ("id"),FOREIGN KEY ("company") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

INSERT INTO "quaint"."new_User" ("company", "dateOfBirth", "email", "firstName", "id", "isActive", "lastName", "password", "tokenVersion") SELECT "company", "dateOfBirth", "email", "firstName", "id", "isActive", "lastName", "password", "tokenVersion" FROM "quaint"."User"

DROP TABLE "quaint"."User";

ALTER TABLE "quaint"."new_User" RENAME TO "User";

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

PRAGMA "quaint".foreign_key_check;

PRAGMA foreign_keys=ON;
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200328134900..20200328155646
--- datamodel.dml
+++ datamodel.dml
@@ -1,7 +1,7 @@
 datasource db {
   provider = "sqlite"
-  url = "***"
+  url      = "file:./dev.db"
 }
 generator client {
   provider = "prisma-client-js"
@@ -42,5 +42,6 @@
   isActive     Boolean   @default(true)
   posts        Post[]
   company      Company   @relation(references: [id])
   teams        Team[]
+  isAdmin      Boolean   @default(false)
 }
```


