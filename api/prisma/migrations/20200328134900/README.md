# Migration `20200328134900`

This migration has been generated by adam-wheatley at 3/28/2020, 1:49:00 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "quaint"."Post" (
    "author" TEXT   ,
    "content" TEXT   ,
    "id" TEXT NOT NULL  ,
    "published" BOOLEAN NOT NULL DEFAULT false ,
    "title" TEXT NOT NULL DEFAULT '' ,
    PRIMARY KEY ("id"),FOREIGN KEY ("author") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
) 

CREATE TABLE "quaint"."Company" (
    "id" TEXT NOT NULL  ,
    "name" TEXT NOT NULL DEFAULT '' ,
    PRIMARY KEY ("id")
) 

CREATE TABLE "quaint"."Team" (
    "company" TEXT NOT NULL  ,
    "createdAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    "id" TEXT NOT NULL  ,
    "name" TEXT   ,
    "updatedAt" DATE NOT NULL DEFAULT '1970-01-01 00:00:00' ,
    PRIMARY KEY ("id"),FOREIGN KEY ("company") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

CREATE TABLE "quaint"."User" (
    "company" TEXT NOT NULL  ,
    "dateOfBirth" DATE   ,
    "email" TEXT NOT NULL DEFAULT '' ,
    "firstName" TEXT   ,
    "id" TEXT NOT NULL  ,
    "isActive" BOOLEAN NOT NULL DEFAULT true ,
    "lastName" TEXT   ,
    "password" TEXT NOT NULL DEFAULT '' ,
    "tokenVersion" INTEGER NOT NULL DEFAULT 0 ,
    PRIMARY KEY ("id"),FOREIGN KEY ("company") REFERENCES "Company"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

CREATE TABLE "quaint"."_TeamToUser" (
    "A" TEXT NOT NULL  ,
    "B" TEXT NOT NULL  ,FOREIGN KEY ("A") REFERENCES "Team"("id") ON DELETE CASCADE ON UPDATE CASCADE,
FOREIGN KEY ("B") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE
) 

CREATE UNIQUE INDEX "quaint"."User.email" ON "User"("email")

CREATE UNIQUE INDEX "quaint"."_TeamToUser_AB_unique" ON "_TeamToUser"("A","B")

CREATE  INDEX "quaint"."_TeamToUser_B_index" ON "_TeamToUser"("B")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200328134900
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,46 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:./dev.db"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Post {
+  id        String  @default(cuid()) @id
+  title     String
+  content   String?
+  published Boolean @default(false)
+  author    User?   @relation(references: [id])
+}
+
+model Company {
+  id    String @default(cuid()) @id
+  name  String
+  users User[]
+  teams Team[]
+}
+
+model Team {
+  id        String   @default(cuid()) @id
+  createdAt DateTime @default(now())
+  updatedAt DateTime @updatedAt
+  name      String?
+  users     User[]   @relation(references: [id])
+  company   Company  @relation(references: [id])
+}
+
+model User {
+  id           String    @default(cuid()) @id
+  email        String    @unique
+  password     String
+  firstName    String?
+  lastName     String?
+  dateOfBirth  DateTime?
+  tokenVersion Int       @default(0)
+  isActive     Boolean   @default(true)
+  posts        Post[]
+  company      Company   @relation(references: [id])
+  teams        Team[]
+}
```


