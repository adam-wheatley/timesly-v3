import React from 'react';
import { Wrapper } from '../../styles/PageStyles';

export const Loading: React.FC = () => {
    return (
        <Wrapper>
            Loading...
        </Wrapper>
    )
}
